# Images

Phase 1 scaffold placeholder. The following assets are embedded in the frozen
v1.4 Google Doc snapshot and are extracted during Phase 2 content migration:

- Hero image (cover art)
- GitHub organisation avatar (deterministic-agents)

They are not yet on disk, so they are not referenced by the build. The cover
page (`chapters/00-cover.qmd`) renders as text until they are added.
