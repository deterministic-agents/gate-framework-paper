# The Layered Architecture

Placeholder. Content lands in Phase 2.

## Cross-reference Target {#sec-test}

This section exists to verify that cross-references resolve in both PDF and
HTML output. It is removed in Phase 2.

```{mermaid}
%%| label: fig-arch-test
%%| fig-cap: "Mermaid render verification diagram."
flowchart LR
  A[Identity and Integrity] --> B[Runtime and Constraints]
  B --> C[Observability and Forensics]
  C --> D[Governance and Lifecycle]
```
